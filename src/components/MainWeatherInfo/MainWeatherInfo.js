import React from 'react';
import { Container } from 'semantic-ui-react';
import classes from './MainWeatherInfo.module.css';

const mainWeatherInfo = () => <Container className={classes.Container} />;

export default mainWeatherInfo;
